#!/bin/sh

rm -f /var/luci-indexcache*
rm -f /tmp/luci-indexcache*

# Register the new menu + rpcd ucode plugin without a reboot.
[ -x /etc/init.d/rpcd ] && /etc/init.d/rpcd reload

logger -t "__PKG_NAME__" "luci-app uci-defaults script executed"

exit 0
